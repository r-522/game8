# クラス図 — 喧嘩番長3 全国制覇

## Mermaid classDiagram

```mermaid
classDiagram
    class GameManager {
        +GameState currentState
        +Player player
        +StageManager stageManager
        +AudioSystem audioSystem
        +SaveSystem saveSystem
        +initialize() void
        +update(deltaTime: float) void
        +changeState(newState: GameState) void
        +shutdown() void
    }

    class StateManager {
        +Map~string, IGameState~ states
        +IGameState currentState
        +registerState(name: string, state: IGameState) void
        +transitionTo(name: string) void
        +update(dt: float) void
    }

    class IGameState {
        <<interface>>
        +onEnter() void
        +onUpdate(dt: float) void
        +onExit() void
    }

    class FieldState {
        +FieldSystem fieldSystem
        +PlayerController playerController
        +NPCManager npcManager
        +onEnter() void
        +onUpdate(dt: float) void
        +onExit() void
    }

    class CombatState {
        +CombatSystem combatSystem
        +bool isPlayerTurn
        +onEnter() void
        +onUpdate(dt: float) void
        +onExit() void
        +checkVictory() bool
        +checkDefeat() bool
    }

    class MenchiState {
        +MenchiSystem menchiSystem
        +Character challenger
        +onEnter() void
        +onUpdate(dt: float) void
        +onExit() void
    }

    class TankaState {
        +TankaSystem tankaSystem
        +TankaDialogue currentDialogue
        +onEnter() void
        +onUpdate(dt: float) void
        +onExit() void
        +evaluateAnswer(choiceIndex: int) TankaResult
    }

    class Player {
        +string name
        +string school
        +string hometown
        +int level
        +int hp
        +int maxHp
        +int power
        +int guard
        +int otokogi
        +int kenkaDamashii
        +List~SpecialMove~ specialMoves
        +List~Character~ peonsSubordinates
        +attack(type: AttackType) void
        +takeDamage(damage: int) void
        +heal(amount: int) void
        +gainExp(exp: int) void
        +levelUp() void
        +launchSpecial(move: SpecialMove) void
        +callPeon(peonIndex: int) void
    }

    class Character {
        +int id
        +string name
        +string prefecture
        +int hp
        +int maxHp
        +int power
        +int guard
        +CharacterType type
        +SpecialMove signature
        +Animator animator
        +Collider hitbox
        +attack(target: Character) void
        +takeDamage(damage: int) void
        +isDefeated() bool
        +getDialogue(type: DialogueType) string
    }

    class Enemy {
        +EnemyAI ai
        +EnemyType enemyType
        +int otokogidrop
        +string dialect
        +TankaDialogue tanka
        +update(dt: float) void
        +decideAction() EnemyAction
    }

    class Boss {
        +int phase
        +int phaseThreshold
        +List~BossPattern~ patterns
        +void onPhaseChange()
        +BossPattern getCurrentPattern()
    }

    class CombatSystem {
        +List~Character~ combatants
        +ComboManager comboManager
        +HitDetection hitDetection
        +int comboCount
        +startCombat(player: Player, enemy: Enemy) void
        +processAttack(attacker: Character, type: AttackType) void
        +resolveHit(attacker: Character, defender: Character, atk: AttackType) void
        +endCombat(winner: Character) void
        +calculateDamage(atk: int, def: int, multiplier: float) int
    }

    class ComboManager {
        +List~AttackType~ comboBuffer
        +int maxComboLength
        +float comboWindowTimer
        +addInput(type: AttackType) bool
        +evaluateCombo() ComboResult
        +resetCombo() void
        +getComboMultiplier() float
    }

    class HitDetection {
        +checkHit(attacker: Character, defender: Character) bool
        +getHitboxOverlap(a: Collider, b: Collider) bool
    }

    class MenchiSystem {
        +float playerGauge
        +float enemyGauge
        +float timerRemaining
        +pushGauge(side: Side, amount: float) void
        +update(dt: float) void
        +getResult() MenchiResult
    }

    class TankaSystem {
        +TankaDatabase database
        +TankaDialogue currentDialogue
        +loadDialogue(enemyId: int) void
        +presentChoices() List~string~
        +submitAnswer(index: int) TankaResult
        +getHint() string
    }

    class TankaDatabase {
        +Map~int, TankaDialogue~ dialogues
        +TankaDialogue get(enemyId: int)
    }

    class TankaDialogue {
        +int enemyId
        +string prefecture
        +string dialectText
        +List~string~ choices
        +int correctIndex
        +TankaType type
    }

    class OtokogiSystem {
        +int currentValue
        +OtokogiRank getRank()
        +addValue(action: OtokogiAction) void
        +subtractValue(action: OtokogiAction) void
        +getEffects() OtokogiEffects
    }

    class StageManager {
        +Stage currentStage
        +List~Stage~ allStages
        +List~NPC~ npcs
        +TimeManager timeManager
        +loadStage(stageId: int) void
        +update(dt: float) void
        +spawnNPC(npcData: NPCData) NPC
        +checkBossConditions() bool
    }

    class TimeManager {
        +int currentDay
        +int currentHour
        +int currentMinute
        +bool isFreePeriod
        +advanceTime(minutes: int) void
        +isNight() bool
        +getDayPhaseName() string
    }

    class ProgressSystem {
        +bool[47] prefectureDefeated
        +Map~int, bool~ eventFlags
        +int defeatedBossCount
        +string unlockedEnding
        +markDefeated(prefectureIndex: int) void
        +setFlag(flagId: int) void
        +checkEndingCondition() EndingType
    }

    class SaveSystem {
        +int maxSlots
        +SaveData loadSave(int slot)
        +void writeSave(int slot, SaveData data)
        +bool slotExists(int slot)
        +deleteSave(int slot) void
    }

    class SaveData {
        +int playerLevel
        +int playerHp
        +int otokogi
        +bool[47] prefectureFlags
        +Map~int, bool~ eventFlags
        +List~int~ unlockedMoves
        +int playTimeSeconds
        +int currentDay
    }

    class AudioSystem {
        +void playBGM(bgmId: int)
        +void stopBGM()
        +void playSE(seId: int)
        +void playVoice(voiceId: int)
        +void setVolume(type: AudioType, vol: float)
        +void fadeBGM(targetVol: float, duration: float)
    }

    GameManager *-- Player
    GameManager *-- StageManager
    GameManager *-- AudioSystem
    GameManager *-- SaveSystem
    GameManager *-- StateManager
    StateManager *-- IGameState
    IGameState <|.. FieldState
    IGameState <|.. CombatState
    IGameState <|.. MenchiState
    IGameState <|.. TankaState
    CombatState *-- CombatSystem
    MenchiState *-- MenchiSystem
    TankaState *-- TankaSystem
    CombatSystem *-- ComboManager
    CombatSystem *-- HitDetection
    TankaSystem *-- TankaDatabase
    TankaDatabase *-- TankaDialogue
    Character <|-- Player
    Character <|-- Enemy
    Enemy <|-- Boss
    StageManager *-- TimeManager
    GameManager *-- ProgressSystem
    SaveSystem *-- SaveData
    Player *-- OtokogiSystem
```

---

## クラス間の関係説明

| 関係 | クラスA | クラスB | 説明 |
|------|---------|---------|------|
| 継承 | Player | Character | プレイヤーはキャラクターの特殊化 |
| 継承 | Enemy | Character | 敵もキャラクターの特殊化 |
| 継承 | Boss | Enemy | ボスは敵の特殊化（フェーズ管理追加） |
| 実装 | FieldState | IGameState | フィールドステートはインターフェース実装 |
| 実装 | CombatState | IGameState | 戦闘ステートはインターフェース実装 |
| 集約 | GameManager | Player | ゲーム全体でプレイヤーを管理 |
| 集約 | CombatSystem | ComboManager | 戦闘システムがコンボ管理を内包 |
| 依存 | TankaSystem | TankaDatabase | タンカシステムはDBからデータ取得 |
| 依存 | ProgressSystem | SaveData | 進行状況はセーブデータに保存される |
